import WidgetBar from '@/components/Home/WidgetBar/WidgetBar';
import { Layout } from '@/components/Layout/Layout';

export default function Home() {
    return (
        <Layout>
            <WidgetBar />
        </Layout>
    );
}
